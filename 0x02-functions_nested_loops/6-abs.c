#include "main.h"
/**
*_abs - _abs block
*@r: int type number
*Description: computes the absolute value of integer
*Return: 0
*/
int _abs(int r)
{
if (r > 0)
{
return (r);
}
else
{
return (-1 * r);
}
}
